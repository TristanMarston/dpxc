const GridView = ({ collectionData, setCollectionData, schema }: { collectionData: any[]; setCollectionData: React.Dispatch<React.SetStateAction<any[]>>; schema: any[] }) => {
    return (
        <div>
            <h4>grid</h4>
        </div>
    );
};

export default GridView;
