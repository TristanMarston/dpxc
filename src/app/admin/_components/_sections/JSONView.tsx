const JSONView = ({ collectionData, setCollectionData, schema }: { collectionData: any[]; setCollectionData: React.Dispatch<React.SetStateAction<any[]>>; schema: any[] }) => {
    return (
        <div>
            <h4>json</h4>
        </div>
    )
}

export default JSONView;